import { ChangePasswordReq } from './change-password-req.model';

describe('ChangePasswordReq', () => {
  it('should create an instance', () => {
    expect(new ChangePasswordReq()).toBeTruthy();
  });
});
