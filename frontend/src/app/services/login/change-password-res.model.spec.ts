import { ChangePasswordRes } from './change-password-res.model';

describe('ChangePasswordRes', () => {
  it('should create an instance', () => {
    expect(new ChangePasswordRes()).toBeTruthy();
  });
});
